# Announcements API application

A small serverless application (MicroService) which allows storing and retrieving announcements.

Implemented as AWS lambda function + DynamoDB

Run:
``` 
sam build 
sam local start-api 
python test.py
```
